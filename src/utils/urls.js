import { domain } from "./domain";

const apiGeneral = {
  login: `${domain}/login`,
  signup: `${domain}/signup`,
  createPod: `${domain}/create`,
  joinPod: `${domain}/join`,
};

export { apiGeneral };
